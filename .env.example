NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

OPENAI_API_KEY=
TAVILY_API_KEY=

# lowestAlert base URL (optional, for /api/pricing/lowest proxy)
LOWEST_ALERT_API_BASE=http://127.0.0.1:3001
# Upstream fetch timeout (ms), default 15000
PRICING_UPSTREAM_TIMEOUT_MS=15000

# API rate limits (per client IP per minute). Set DISABLE_API_RATE_LIMIT=true for local dev stress tests.
# DISABLE_API_RATE_LIMIT=true
API_RATE_LIMIT_ANALYZE_PER_MINUTE=30
API_RATE_LIMIT_PRICING_PER_MINUTE=60

# Topic brief cache TTL (ms), optional
# TOPIC_BRIEF_TTL_MS=600000
